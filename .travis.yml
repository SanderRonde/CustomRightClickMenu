language: node_js
node_js: "node"
branches:
  only:
    - master
    - beta
before_install: 
  - npm install grunt -g
install:
  - npm install
script: sh ./build.sh
notifications:
  email:
    on-success: change
    on_failure: change
