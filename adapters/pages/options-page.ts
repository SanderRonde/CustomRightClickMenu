// Take HTML file and JSON stringify it
// runtime.openOptionsPage() instead opens some URL 
// intercept that URL and return the HTML file
// inline everything in the HTML file
export const OPTIONS_HTML_FILE = '//TODO: fill with options.html content';